# config.yaml — this file IS committed to the repo (it holds no secrets;
# the chat API's key/base URL/model all come from env vars/GH secrets at
# load time). The GitHub Actions workflow reads this exact file.

channels:                       # usernames, no @ and no t.me/, each with a category
  - { name: darvishnameh, category: analysis }
  - { name: yaser_arab57, category: analysis }
  - { name: ahmadzeidabad, category: analysis }
  - { name: mahdi_mohammadi61, category: analysis }
  - { name: RezaEjtehadi, category: analysis }
  - { name: mostafamalekian, category: analysis }
  - { name: javadimr, category: analysis }
  - { name: sherwin_vakili, category: analysis }  # web preview disabled on this channel, see note below
  - { name: miladdokhanchi, category: analysis }
  - { name: masoudnili, category: analysis }      # web preview disabled on this channel, see note below
  - { name: vahidkarimipour, category: analysis }
  - { name: I_D_Network, category: analysis }
  - { name: Renani_Mohsen, category: analysis }
  - { name: fazelimohammadir, category: analysis }
  - { name: sharenovate, category: analysis }
  - { name: ermiadotir, category: analysis }
  - { name: bamotamem, category: analysis }
  - { name: jafarshiralinia, category: analysis }
  - { name: mohsenhesammazaheri, category: analysis }
  - { name: jalaeipour, category: analysis }
  - { name: bijanabdolkarimi, category: analysis }
  - { name: FarnazSeifi, category: analysis }
  - { name: hesmashena, category: analysis }
  - { name: gpttir, category: analysis }
  - { name: irandarhalegozar, category: analysis }  # web preview disabled on this channel, see note below
  - { name: abdiabbas, category: analysis }
  - { name: Mostafa_Najafii, category: analysis }   # web preview disabled on this channel, see note below
  - { name: mmaljoo, category: analysis }
  - { name: Soroushdabbagh_Official, category: analysis }
  - { name: VahidOnline, category: news }
  - { name: sahandiranmehr, category: analysis }
  - { name: iranstrategicnarratives, category: analysis }
  - { name: sadegh_alhosseini, category: analysis }
  - { name: Ghabl_enghelab, category: analysis }
  - { name: aqvalanam, category: analysis }
  - { name: mamlekate, category: news }
  - { name: javadmogoei, category: analysis }
  - { name: Kayhan_online, category: news }
  - { name: eslahatnews, category: news }
  - { name: jebheheslahat, category: news }
  - { name: mersadcss, category: news }
  - { name: FarsNA, category: news }
  # Channels above marked "web preview disabled" are real, valid
  # usernames, but mode: web can't fetch them — see "A channel isn't
  # producing any posts" in the README. Left in the list so they pick
  # up automatically if that ever changes.

source:
  mode: web                     # "web" | "mtproto". Default "web". Use "web" for CI.
  # --- only used when mode == mtproto ---
  telegram:
    api_id: ${TG_API_ID}
    api_hash: ${TG_API_HASH}
    session_file: ./tg.session

translate:
  api_key: ${TRANSLATE_API_KEY}       # required — GH secret, OpenAI-compatible chat API key
  base_url: ${TRANSLATE_API_BASE_URL} # required — GH secret, e.g. https://api.your-provider.example.com
  model: ${TRANSLATE_API_MODEL}       # required — GH secret, the model name your provider expects
  max_chars_per_post: 4096      # posts longer than this are skipped, not truncated (4096 = Telegram's own message-length cap)
  target_language: English
  temperature: 0.2

output:
  dir: ./public/feeds           # XML written here
  base_url: https://oshintanakura.github.io/iran-rss-feed/feeds  # your GitHub Pages URL
  per_channel_feeds: true       # write <username>.xml for each channel
  combined_feed: true           # write all.xml with every channel merged
  max_feed_age_days: 15         # every translated post newer than this stays in its feed, no matter how many that is
  max_items_per_feed: 2000      # hard ceiling on feed size, purely against a pathological runaway — should never actually bind
  include_original: false       # append the Persian original inside the item body

state:
  path: ./state.db              # sqlite file

runtime:
  fetch_timeout_seconds: 30
  translate_timeout_seconds: 120
  max_new_posts_per_run: 200    # safety valve on first run / backfill
  max_post_age_days: 7          # ignore posts older than this, every fetch, not just first backfill
  dry_run: false                # when true: fetch + report, never call the chat API, never write XML
